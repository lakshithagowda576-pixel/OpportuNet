{
  "name": "opportunet-deployment",
  "version": "1.0.0",
  "description": "OpportuNet deployment package",
  "main": "api/[[...all]].ts",
  "scripts": {
    "build": "echo 'Building OpportuNet...' && cd artifacts/job-portal && npm install && npm run build",
    "dev": "echo 'Development mode'",
    "start": "echo 'Starting OpportuNet'"
  },
  "dependencies": {
    "serverless-http": "^2.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
